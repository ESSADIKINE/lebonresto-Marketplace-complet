'use client';

import React from 'react';

/**
 * NavbarLight has been replaced by the global two-level header in layout.jsx.
 * This component now renders nothing to ensure no duplication occurs on pages 
 * that still import it.
 * 
 * TODO: Remove usages of <NavbarLight /> from all page files and then delete this component.
 */
export default function NavbarLight() {
    return null;
}
